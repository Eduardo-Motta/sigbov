export function setFarm (state, farm) {
  state.farm.data = farm
}

export function setAddress (state, address) {
  state.address = address
}

export function setPasture (state, pasture) {
  state.pasture = pasture
}

export function setAnimal (state, animal) {
  state.animal = animal
}

export function setAnimalBreed (state, animalBreed) {
  state.animalBreed.data = animalBreed
}

export function setStockAlert (state, stockAlert) {
  state.stockAlert = stockAlert
}
